{
  "1": [
    "# Task",
    "",
    "Create a file named `hello.py` in this directory. Its entire contents should be:",
    "",
    "```text",
    "hello sentinel",
    "```"
  ],
  "2": [
    "# Task",
    "",
    "Create `math_utils.py` with two functions:",
    "",
    "- `add(a, b)` returns the sum.",
    "- `subtract(a, b)` returns the difference.",
    "",
    "Keep the implementation small and readable."
  ],
  "3": [
    "# Task",
    "",
    "Create a `notes` directory containing `summary.md`.",
    "",
    "The summary should have a top-level heading `# Summary` and one bullet that says `- Bench task complete`."
  ],
  "4": [
    "# Task",
    "",
    "Create `config.json` with this exact JSON object:",
    "",
    "```json",
    "{",
    "  \"name\": \"sentinel-bench\",",
    "  \"enabled\": true,",
    "  \"retries\": 2",
    "}",
    "```"
  ],
  "5": [
    "# Task",
    "",
    "Create `greet.json` with a function named `greet(name)` that returns `Hello, <name>!`.",
    "",
    "For example, `greet(\"Ada\")` should return `Hello, Ada!`."
  ]
}
