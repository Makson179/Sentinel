Это наши мысли по всем восьми фиксам, для обсуждения перед реализацией. Если у тебя тоже не возникнет вопросов и мы ничего не упускаем, чиним все восемь одним батчем, не в две волны. Причина: ни один из пунктов не меняет планку суждения супервизора, это всё механика, инфра, учёт и безопасность, а breadth (то что реально измеряли) уже зашит. Стейджить незачем.
Сначала главное, оно важнее всех восьми пунктов вместе.
Универсальность. Супервизор и вся система должны оставаться универсальными под любую задачу, не только SpecBench, а и веб-приложение, CLI-тулза, библиотека, что угодно. Ни один фикс не должен привязывать систему к pytest или к структуре "public плюс hidden". В частности канал behavior_demo обязан принимать сырой вывод любой команды, а не требовать pytest. Прямой вопрос к тебе: ты это понимаешь, и останется ли система универсальной (не pytest- и не bench-специфичной) после этих фиксов? Если какой-то из пунктов это нарушает, скажи прямо до реализации.
Теперь сами фиксы.

behavior_demo, first-class универсальный канал (главный). Кодер помечает любую команду которая реально дёргает изменённый артефакт и выдаёт сырой вывод как demo, контроллер кладёт этот вывод в ledger привязанным к behavior, reviewer судит вывод против спеки. Доверие без pytest: дешёвая детерминированная проверка что demo-команда реально исполняет изменённый артефакт (а не echo ожидаемой строки), плюс reviewer критично читает сырой вывод. pytest это одна из допустимых форм, не обязательная. Плюс контракт в промпте кодера, тоже универсальный ("запусти команду которая исполняет изменённую штуку и выдаёт сырой вывод, пометь как demo"), не "напиши pytest-тест". Это чинит рекуррентный баг с нераспознаванием raw-demo (hello-world, sql targeted test с validation_id=null).
inspection_id, дотянуть end-to-end. Read-only наблюдение получает inspection-запись в ledger, reviewer привязывает anti-hack/static строки к inspection_id, accept-gate принимает inspection_id как валидное доказательство для static/source ограничений и только для них, не для runtime behavior. Кейс где anti-hack строка осталась без валидной записи должен биндиться к inspection_id, а не падать в audit failure.
JSON robustness. Обернуть парсинг structured output супера в retry/repair/sanitize, кривой JSON перезапросить или починить, а не валить ран в provider_failure. И ограничить длину вывода чтобы не обрывался посреди строки.
Счётчики/логи. Один источник правды по метрикам (runtime intervene, completion return, restart, approval, accept), и нормально логировать completion/restart в events.jsonl, чтобы final report и лог сходились.
read-only large_diff. Read-only команда не меняет дифф, значит не должна триггерить large_diff или restart_budget wake. Завязать этот wake на реальное изменение диффа, исключить read-only из триггера.
Cleanup. Хелпер очистки обязан проверять git tracked/untracked до rm -rf. Tracked не трогаем никогда, удаляем только untracked мусор, не полагаясь что супер поймает каждый раз. Это риск потери данных.
Reporting. Разделить финальный статус на два поля: "SpecBench scoring" (валидный scored run, score) и "controller acceptance" (accepted/escalated/provider_failure), чтобы валидность скора была видна отдельно от того подписал ли супер.

Скажи где это упирается в код, есть ли у тебя вопросы, и подтверди по универсальности. Если вопросов нет и ничего не упустили, чиним все восемь сразу.




Три вопроса по правкам, первый главный.

По behavior_demo и "абсолютный python". Распознавание demo завязано на python, или оно ключится на маркер SENTINEL_BEHAVIOR_DEMO=1 плюс детерминированную проверку "команда реально дёргает изменённый артефакт"? Канал обязан быть универсальным: demo через curl (веб), node, бинарь или shell-команду к API должен засчитываться так же. Absolute python это максимум анти-masking хардинг для питоновского случая, а не условие что demo обязан быть на python. Подтверди что универсальность не потеряна.
В твоём списке нет пунктов 5 (унификация счётчиков/логов) и 6 (read-only не будит large_diff, wake завязан на изменение diff signature). Они вошли в этот батч или выпали? Договаривались про все восемь.
По inspection_id auto-recovery: когда контроллер восстанавливает единственную inspection-запись по команде (reviewer забыл id), он делает это только если кандидат ровно один и однозначен? Если кандидатов несколько, он не должен сам выбирать, иначе привяжет не ту inspection к static-строке и создаст ложное covered.