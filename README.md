# Supervisor Agent MVP

Supervisor is a CLI wrapper that supervises Claude Code or Codex CLI runs with
authenticated hooks, deterministic policy, LLM-backed gray-zone decisions,
stateful health tracking, and kill/restart handoff support.

See `PLAN.md` for the MVP specification.

