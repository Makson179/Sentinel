[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Bello"
version = "0.3.1"
description = "Terminal supervisor for autonomous Codex app-server runs"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
  { name = "Maxim Bocharov" },
]
dependencies = [
  "click>=8.1",
  "packaging>=24.0",
  "pydantic>=2.0",
  "prompt-toolkit>=3.0",
  "wcwidth>=0.2",
]

[project.urls]
Homepage = "https://github.com/Makson179/Bello"
Repository = "https://github.com/Makson179/Bello"
Issues = "https://github.com/Makson179/Bello/issues"

[project.optional-dependencies]
test = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
]

[project.scripts]
bello = "supervisor.main:cli"

[tool.setuptools.packages.find]
include = ["supervisor*"]

[tool.setuptools.package-data]
"supervisor" = ["_vendor/context_mode/**/*"]
"supervisor.prompts" = ["*.toml"]
"supervisor.context_mode" = ["manifests/*.json", "native-release.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
